monitor = HDMI-A-2,preferred,auto,2.4

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = kitty
exec-once = waybar

input {
	kb_layout = br
}

general {
	gaps_in = 5
	gaps_out= 10
	border_size = 2
}

decoration {
	rounding = 8
}

animations {
	enabled = yes
}

# Some default env vars.
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = QT_QPA_PLATFORMTHEME,qt5ct # change to qt6ct if you have that 
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = GDK_BACKEND,wayland,x11,*
env = SDL_VIDEO_DRIVER,wayland
env = MOZ_ENABLE_WAYLAND,1

$fileManager = dolphin
$menu = wofi --show drun --allow-images
$mainMod = SUPER
$monitor1 = "DP-6"
$monitor1Resolution = "3440X1440@165"
$monitor2 = "HDMI-A-2"
$monitor2Resolution = "3840x2160@60"

bind = $mainMod,Escape,exec, killall waybar || waybar
bind = $mainMod,T,exec,kitty
bind = $mainMod,E,exec,$fileManager
bind = $mainMod,f,toggleFloating
bind = $mainMod,A,exec,$menu
bind = $mainMod,C,killactive
bind = $mainMod,X,exit
bind = $mainMod,M,fullscreen
bind = $mainMod,F1,exec,hyprctl keyword monitor "$monitor2,disable" && sleep 4 && hyprctl keyword monitor "$monitor1,preferred,$monitor1Resolution,1"
bind = $mainMod,F2,exec,hyprctl keyword monitor "$monitor1,disabled" && sleep 4 && hyprctl keyword monitor "$monitor2,preferred,$monitor2Resolution,2.4"

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d 

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10 

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 
❯ vim .config/hypr/hyprland.conf
❯ cat .config/hypr/hyprland.conf
monitor = HDMI-A-2,preferred,auto,2.4

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = kitty
exec-once = waybar

input {
	kb_layout = br
}

general {
	gaps_in = 5
	gaps_out= 10
	border_size = 2
}

decoration {
	rounding = 8
}

animations {
	enabled = yes
}

# Some default env vars.
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = QT_QPA_PLATFORMTHEME,qt5ct # change to qt6ct if you have that 
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland
env = GDK_BACKEND,wayland,x11,*
env = SDL_VIDEO_DRIVER,wayland
env = MOZ_ENABLE_WAYLAND,1

$fileManager = dolphin
$menu = wofi --show drun --allow-images
$mainMod = SUPER
$monitor1 = "DP-6"
$monitor1Resolution = "3440X1440@165"
$monitor2 = "HDMI-A-2"
$monitor2Resolution = "3840x2160@60"

bind = $mainMod,Escape,exec, killall waybar || waybar
bind = $mainMod,T,exec,kitty
bind = $mainMod,E,exec,$fileManager
bind = $mainMod,f,toggleFloating
bind = $mainMod,A,exec,$menu
bind = $mainMod,C,killactive
bind = $mainMod,X,exit
bind = $mainMod,M,fullscreen
bind = $mainMod,F1,exec,hyprctl keyword monitor "$monitor2,disable" && sleep 4 && hyprctl keyword monitor "$monitor1,preferred,$monitor1Resolution,1"
bind = $mainMod,F2,exec,hyprctl keyword monitor "$monitor1,disabled" && sleep 4 && hyprctl keyword monitor "$monitor2,preferred,$monitor2Resolution,2.4"

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d 

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10 

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10 

# Move active window
bind = SUPER SHIFT, left, movewindow, l
bind = SUPER SHIFT, right, movewindow, r
bind = SUPER SHIFT, up, movewindow, u
bind = SUPER SHIFT, down, movewindow, d
